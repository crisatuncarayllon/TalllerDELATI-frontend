import { SearchClusterPipe } from './search-cluster.pipe';

describe('SearchClusterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchClusterPipe();
    expect(pipe).toBeTruthy();
  });
});
